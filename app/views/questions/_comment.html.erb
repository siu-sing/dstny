<div>
    <h4>Comment by <%= comment.user_id%></h4>
    <p class="comment"><%= comment.comment_text %></p>
    <p>Posted <%= distance_of_time_in_words(comment.question.created_at, comment.created_at) %>
</div>